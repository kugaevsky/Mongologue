// todo: эффект должен совпадать с "html вариантом", т.е. не должно отображаться более одной страницы
// удалять предыдущие страницы?

$('<% @posts.each do |post| %>\
    <% cache(:action => "index", :id => post.pid, :action_suffix => authorized_admin? ) do %>\
       <%= escape_javascript render post %>\
       <div id=<%= "pc#{post.pid}" %> style="display: none;" ></div>\
    <% end %>\
  <% end %>').appendTo("#posts").hide().slideDown();
$('#paginator').html('<%= escape_javascript(link_to_next_page(@posts, "&darr; MORE POSTS &darr;".html_safe, :remote => true)) %>');
